
# --- Phase 6.1: Sentry (optional — app runs fully without) ---
# Provision via the Antigravity prompt; server sends runtime errors/perf only.
SENTRY_DSN=
# SENTRY_AUTH_TOKEN=   # only if uploading sourcemaps from CI (hideSourceMaps is on)
